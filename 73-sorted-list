public boolean isSorted(List<Integer> numbers) {
    //code here
    Boolean truth = true;
    List<Integer> checkList = numbers;
    checkList.sort();
    for(Integer i = 0; i < numbers.size(); i++){
        if(numbers[i] != checkList[i]){
            truth = false;
        }
    }
    return truth;
}