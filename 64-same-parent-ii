public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here
    if(opp.AccountId == acc.Id && con.AccountId == acc.Id){
        return true;
    }
    else{
        return false;
    }
}