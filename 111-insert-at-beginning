public void insertAtStart(String cityName, List<String> cities) {
    //code here
    for(Integer i = 0; i < cities.size(); i++){
        if(i == (cities.size()-1){
            cities.add(cities[i]);
        }
        else{
            cities.set(i+1, cities[i]);
        }
    }
    cities.set(0,cityName);
    
}