public List<String> fullNames(List<String> firstNames, List<String> lastNames) {
    //code here
    List<String> bothNames = new List<String>();
    Integer index = 0;
    for(String s : firstNames){
        bothNames.add(s + ' ' + lastNames.get(index));
        index++;
    }
    return bothNames;
}